[project]
name = "python-base"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sharocal", email = "1056154328@qq.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
python-base = "python_base:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"
